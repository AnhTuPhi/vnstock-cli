[project]
name = "vnstock-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.0.0",
    "rich>=15.0.0",
    "typer>=0.23.2",
    "vnstock>=3.4.0",
]
